<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/ec_favourite_image"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentLeft="true"
            android:layout_alignParentStart="true"
            android:layout_alignParentTop="true"
            android:layout_marginLeft="28dp"
            android:layout_marginStart="28dp"
            android:scaleType="centerCrop"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:srcCompat="@drawable/pumpkinpiefrenchtoast" />

        <TextView
            android:id="@+id/ec_favourite_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:minHeight="50dp"
            android:layout_alignBottom="@id/ec_favourite_image"
            android:layout_alignEnd="@id/ec_favourite_image"
            android:layout_alignLeft="@id/ec_favourite_image"
            android:layout_alignRight="@id/ec_favourite_image"
            android:layout_alignStart="@id/ec_favourite_image"
            android:background="#111111"
            android:alpha="0.8"
            android:textColor="#fafafa"
            android:text="TextView"
            android:visibility="visible" />
    </RelativeLayout>

    <TextView
        android:id="@+id/ec_favourites_publisher"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <TextView
        android:id="@+id/ec_favourites_ingredients"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="TextView" />

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/ec_favourite_delete_icon"
        android:src="@drawable/ic_delete_black_24dp"/>

</android.support.constraint.ConstraintLayout>